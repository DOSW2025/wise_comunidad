# 🗂️ Diagrama de Arquitectura - Módulo de Foros

## Arquitectura en Capas

```
┌─────────────────────────────────────────────────────────────────┐
│                      CLIENTE HTTP                               │
│              (Navegador / Cliente REST)                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    POST /api/foros/forums
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   PRESENTATION LAYER                            │
│                                                                 │
│    ┌──────────────────────────────────────────────────────┐   │
│    │         ForosController (10 endpoints)              │   │
│    ├──────────────────────────────────────────────────────┤   │
│    │ • POST /forums → createForo()                       │   │
│    │ • GET /forums → listForums()                        │   │
│    │ • POST /forums/:slug/threads → createThread()      │   │
│    │ • GET /forums/:slug/threads → listThreads()        │   │
│    │ • GET /threads/:id → getThread()                   │   │
│    │ • POST /threads/:id/posts → createPost()           │   │
│    │ • POST /forums/:id/messages → sendMensaje()        │   │
│    │ • GET /forums/:id/messages → listMensajes()        │   │
│    │ • POST /messages/:id/read → markAsRead()           │   │
│    │ • GET /forums/:id/messages/unread/count → count()  │   │
│    └──────────────────────────────────────────────────────┘   │
└────────────────────┬─────────────────────────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
         ▼                       ▼
    ┌──────────┐          ┌────────────┐
    │DTO Layer │          │ Validation │
    │          │          │  (class-   │
    │ • Create │          │  validator)│
    │   ForoDto│          │            │
    │ • Create │          │ @IsString()│
    │ThreadDto │          │ @IsNotEmpty│
    │ • Create │          │            │
    │  PostDto │          │            │
    │ • Create │          │            │
    │MensajeDto│          │            │
    └────┬─────┘          └────┬───────┘
         │                     │
         └──────────┬──────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BUSINESS LOGIC LAYER                          │
│                                                                 │
│  ┌────────────────────────────┐  ┌──────────────────────────┐ │
│  │   ForosService             │  │  MensajesService         │ │
│  ├────────────────────────────┤  ├──────────────────────────┤ │
│  │ • createForo()             │  │ • sendMensaje()          │ │
│  │ • listForums()             │  │ • listMensajes()         │ │
│  │ • createThread()           │  │ • markAsRead()           │ │
│  │ • listThreads()            │  │ • getUnreadCount()       │ │
│  │ • getThread()              │  │                          │ │
│  │ • createPost()             │  │                          │ │
│  │                            │  │                          │ │
│  │ Validaciones:              │  │ Validaciones:            │ │
│  │ ✓ Materia existe           │  │ ✓ Foro existe            │ │
│  │ ✓ Slug único               │  │ ✓ Usuario existe         │ │
│  │ ✓ Un foro por materia      │  │ ✓ Contenido válido       │ │
│  └──────────┬─────────────────┘  └────────┬─────────────────┘ │
│             │                             │                    │
│             └──────────────┬──────────────┘                    │
│                            │                                   │
│                    ┌───────▼────────┐                         │
│                    │  PrismaService │                         │
│                    │  (Inyectado)   │                         │
│                    └───────┬────────┘                         │
└─────────────────────────────┼──────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│              INFRASTRUCTURE/DATA ACCESS LAYER                   │
│                                                                 │
│    ┌────────────────────────────────────────────────────────┐  │
│    │           Prisma Client                               │  │
│    │  (Objeto global que conecta con PostgreSQL)           │  │
│    └────────┬──────────────────────────────────────────────┘  │
└─────────────┼───────────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE LAYER                                │
│                                                                 │
│  ┌─────────┐  ┌──────────┐  ┌────────┐  ┌──────────────┐      │
│  │  foro   │  │  hilo    │  │ post   │  │   mensaje    │      │
│  ├─────────┤  ├──────────┤  ├────────┤  ├──────────────┤      │
│  │ id (PK) │  │ id (PK)  │  │ id(PK) │  │ id (PK)      │      │
│  │ slug *  │  │ title    │  │ thread │  │ forumId  *   │      │
│  │ nombre  │  │ slug *   │  │ author │  │ authorId     │      │
│  │ desc.   │  │ forumId* │  │ parent │  │ contenido    │      │
│  │ materia*│  │ author * │  │ content│  │ leido        │      │
│  │ dates   │  │ replies  │  │ dates  │  │ dates        │      │
│  └─────────┘  │ isPinned │  └────────┘  └──────────────┘      │
│               │ isLocked │                                      │
│               │ views    │                                      │
│               │ dates    │                                      │
│               └──────────┘                                      │
│                                                                 │
│                    PostgreSQL Database                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## Flujo de Creación de Foro

```
┌─────────────────────────────────────┐
│ 1. POST /api/foros/forums           │
│    Body: {slug, nombre, desc, ...}  │
└────────────────┬────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ 2. ForosController │
        │    recibe request  │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ 3. DTO Valida      │
        │    estructura      │
        │    (class-v)       │
        └────────┬───────────┘
                 │
         ✅ Válido / ❌ Error
         │                   │
         ▼                   ▼
    ┌─────────┐         ┌──────────────┐
    │ Proceder│         │ Lanzar error │
    └────┬────┘         └──────┬───────┘
         │                     │
         ▼                     ▼
  ┌─────────────────────────────────────┐
  │ GlobalExceptionFilter               │
  │ Captura y formatea error            │
  │ → ApiResponse.error()               │
  │ → Response 400 + JSON               │
  └─────────────────────────────────────┘
         │
         ▼
    ┌──────────────────┐
    │ ForosService     │
    │ .createForo()    │
    └────────┬─────────┘
             │
    ┌────────┴──────────────┬────────────┐
    │                       │            │
    ▼                       ▼            ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Validación 1 │  │ Validación 2 │  │ Validación 3 │
│              │  │              │  │              │
│ ¿Materia     │  │ ¿Slug único? │  │ ¿Un foro     │
│  existe?     │  │              │  │  por materia?│
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
    ❌ │404          ❌ │409          ❌ │409
    ✅ │            ✅ │            ✅ │
       │                 │                 │
       └────────┬────────┴────────┬────────┘
                │
                ▼
        ┌──────────────────────┐
        │ TODAS OK → Crear en  │
        │ PrismaService        │
        │ INSERT INTO foro     │
        │ values (...)         │
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ 4. Retornar respuesta│
        │    Status 201        │
        │    + JSON + timestamp│
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ CLIENTE recibe:      │
        │ {                    │
        │  "success": true,    │
        │  "statusCode": 201,  │
        │  "message": "...",   │
        │  "data": { foro },   │
        │  "timestamp": "..."  │
        │ }                    │
        └──────────────────────┘
```

---

## Flujo de Manejo de Excepciones

```
┌────────────────────────────────┐
│   Cualquier Excepción          │
│   En el Sistema                │
└────────────┬───────────────────┘
             │
             ▼
    ┌─────────────────────┐
    │ GlobalException     │
    │ Filter.catch()      │
    └────────┬────────────┘
             │
    ┌────────┴──────────────┬──────────────┬──────────────┐
    │                       │              │              │
    ▼                       ▼              ▼              ▼
┌──────────────┐  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ HttpException│  │ BadRequest   │ │ NotFound     │ │ Conflict     │
│              │  │ Exception    │ │ Exception    │ │ Exception    │
│ statusCode   │  │              │ │              │ │              │
│ 400-599      │  │ 400          │ │ 404          │ │ 409          │
└──────────────┘  └──────────────┘ └──────────────┘ └──────────────┘
    │                   │                  │               │
    │                   │                  │               │
    └───────────┬───────┴──────────┬───────┴───────┬───────┘
                │                  │               │
                ▼                  ▼               ▼
        ┌──────────────────────────────────────────┐
        │ Extraer:                                 │
        │ • statusCode                             │
        │ • message                                │
        │ • errors (si existen)                    │
        │ • path (de request)                      │
        │ • timestamp (actual)                     │
        └──────────┬───────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────────┐
        │ Logger.error()                           │
        │ "Error creating forum"                   │
        │ Contexto: "ForosService"                 │
        │ Stack trace completo                     │
        └──────────┬───────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────────┐
        │ ApiResponse.error()                      │
        │ Genera respuesta estandarizada:          │
        │ {                                        │
        │   "success": false,                      │
        │   "statusCode": 409,                     │
        │   "message": "Ya existe...",             │
        │   "data": null,                          │
        │   "timestamp": "...",                    │
        │   "path": "/api/foros/forums",           │
        │   "errors": [{                           │
        │     "field": "materiaId",                │
        │     "message": "..."                     │
        │   }]                                     │
        │ }                                        │
        └──────────┬───────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────────┐
        │ Enviar response al cliente               │
        │ HTTP Status: 409 Conflict                │
        │ Content-Type: application/json           │
        └──────────────────────────────────────────┘
```

---

## Estructura de Inyección de Dependencias

```
┌─────────────────────────────────────┐
│         app.module.ts               │
│   (Módulo Principal de NestJS)      │
└──────────────┬──────────────────────┘
               │
      ┌────────┴────────┐
      │                 │
      ▼                 ▼
┌─────────────┐   ┌───────────────┐
│ PrismaModule│   │ ForosModule   │
└──────┬──────┘   └────────┬──────┘
       │                   │
       ▼                   ▼
┌─────────────────────────────────────┐
│      PrismaService (Provider)       │
│  • Conexión a Base de Datos         │
│  • Inyectable en otros servicios    │
└──────────┬────────────────────────────┘
           │
  ┌────────┴────────┐
  │                 │
  ▼                 ▼
┌────────────────────────────────┐   ┌──────────────────────┐
│   ForosService (Provider)      │   │ MensajesService      │
│                                │   │ (Provider)           │
│ @Injectable()                  │   │                      │
│ export class ForosService {    │   │ @Injectable()        │
│   constructor(                 │   │ export class Message │
│     private prisma: Prisma     │   │   constructor(       │
│   ) {}                         │   │     private prisma   │
│ }                              │   │   ) {}               │
└────────────┬───────────────────┘   └──────────┬───────────┘
             │                                  │
             └──────────┬───────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────┐
        │  ForosController             │
        │  constructor(                │
        │    private foros: Foros      │
        │    private mensajes: Mensajes│
        │  ) {}                        │
        └──────────────────────────────┘
```

---

## Ciclo de Vida de una Solicitud

```
┌─────────────────────────────────────────────────────────┐
│ 1. RECEPCIÓN: Cliente envía HTTP Request               │
│    POST /api/foros/forums                              │
│    Headers: Content-Type: application/json             │
│    Body: { slug, nombre, descripcion, materiaId }      │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────┴────────────────────────────────────────┐
│ 2. ROUTING: NestJS mapea a endpoint en Controller       │
│    @Post('/forums')                                    │
│    createForo(@Body() dto: CreateForoDto)              │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────┴────────────────────────────────────────┐
│ 3. VALIDACIÓN DTO: class-transformer & class-validator │
│    Valida tipos de datos                               │
│    ❌ Error? → HttpException                           │
│    ✅ OK? → Continuar                                  │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────┴────────────────────────────────────────┐
│ 4. INYECCIÓN: NestJS inyecta ForosService              │
│    (instancia única del servicio)                      │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────┴────────────────────────────────────────┐
│ 5. CONTROL LÓGICA: Servicio ejecuta createForo()      │
│    ├─ Validación 1: Materia existe                    │
│    ├─ Validación 2: Slug único                        │
│    ├─ Validación 3: Un foro por materia               │
│    └─ Crear en BD si todo OK                          │
└────────────────┬────────────────────────────────────────┘
                 │
        ┌────────┴──────────┐
        │                   │
        ▼                   ▼
    ┌─────────┐       ┌──────────────┐
    │ ✅ OK  │       │ ❌ Error     │
    │ Crear  │       │ Lanzar       │
    │ en BD  │       │ excepción    │
    └────┬───┘       └────┬─────────┘
         │                │
         └────────┬───────┘
                  │
┌─────────────────┴──────────────────────────────────────┐
│ 6. RESPUESTA EXITOSA o ERROR                          │
│                                                       │
│  ✅ ForoResponse → Status 201 Created                │
│  ❌ GlobalExceptionFilter.catch()                    │
│     → ApiResponse.error() → Status 4xx/5xx           │
└─────────────────┬──────────────────────────────────────┘
                  │
┌─────────────────┴──────────────────────────────────────┐
│ 7. SERIALIZACIÓN JSON y ENVÍO                         │
│    Response:                                          │
│    {                                                  │
│      "success": true|false,                           │
│      "statusCode": 201|4xx|5xx,                       │
│      "message": "...",                                │
│      "data": {...} | null,                            │
│      "timestamp": "2025-11-28T...",                   │
│      "path": "/api/foros/forums",                     │
│      "errors": [...]  (si aplica)                     │
│    }                                                  │
└─────────────────┬──────────────────────────────────────┘
                  │
┌─────────────────┴──────────────────────────────────────┐
│ 8. CLIENTE recibe respuesta                           │
│    Content-Type: application/json                     │
│    Procesa datos según statusCode                     │
└─────────────────────────────────────────────────────────┘
```

---

## Árbol de Componentes

```
ForosModule
├── Controllers
│   └── ForosController (10 endpoints)
│       ├── POST /forums → ForosService.createForo()
│       ├── GET /forums → ForosService.listForums()
│       ├── POST /forums/:slug/threads → ForosService.createThread()
│       ├── GET /forums/:slug/threads → ForosService.listThreads()
│       ├── GET /threads/:id → ForosService.getThread()
│       ├── POST /threads/:id/posts → ForosService.createPost()
│       ├── POST /forums/:id/messages → MensajesService.sendMensaje()
│       ├── GET /forums/:id/messages → MensajesService.listMensajes()
│       ├── POST /forums/:forumId/messages/:messageId/read → MensajesService.markAsRead()
│       └── GET /forums/:id/messages/unread/count → MensajesService.getUnreadCount()
│
├── Services
│   ├── ForosService
│   │   ├── createForo() → Crea foro con 3-level validation
│   │   ├── listForums() → Paginado (20/página)
│   │   ├── createThread() → Transacción: hilo + primer post
│   │   ├── listThreads() → Paginado por foro
│   │   ├── getThread() → Con todos los posts
│   │   └── createPost() → Con soporte para anidamiento
│   │
│   └── MensajesService
│       ├── sendMensaje() → Crea mensaje
│       ├── listMensajes() → Paginado, opcionalmente sin leer
│       ├── markAsRead() → Marca como leído
│       └── getUnreadCount() → Cuenta sin leer
│
├── DTOs (Validación)
│   ├── CreateForoDto
│   ├── CreateThreadDto
│   ├── CreatePostDto
│   └── CreateMensajeDto
│
└── Providers
    ├── PrismaService (Base de datos)
    └── Logger (NestJS)

GlobalExceptionFilter (Registrado en main.ts)
└── Captura todas las excepciones

ApiResponse (common/responses)
└── Factory de respuestas estandarizadas

ForosMessages (common/messages)
└── Mensajes centralizados de negocio
```

---

## Relaciones de Datos (ERD Simplificado)

```
     ┌──────────────┐
     │   materia    │
     │  (existente) │
     └──────┬───────┘
            │
         1:1│ (unique)
            │
            ▼
       ┌─────────┐
       │  foro   │◄──────────┐
       ├─────────┤           │
       │ id (PK) │           │
       │ slug *  │           │
       │ nombre  │      M:1  │ leído
       │ descrip │      por  │
       │ materia │      foro │
       │ *fecha  │           │
       └────┬────┘           │
       M:1  │            ┌────────────────┐
            │            │   mensaje      │
            │            ├────────────────┤
            │            │ id (PK)        │
            │            │ forumId (FK)*  │
            │            │ authorId (FK)  │
            │            │ contenido      │
            │            │ leido          │
            │            │ *fecha         │
            │            └────────────────┘
            │
            ▼
       ┌─────────┐
       │  hilo   │
       ├─────────┤
       │ id (PK) │
       │ title   │
       │ slug *  │
       │ forum*  │
       │ author* │
       │ replies │
       │ pinned  │
       │ locked  │
       │ views   │
       │ *fecha  │
       └────┬────┘
            │
         M:1│
            │
            ▼
       ┌─────────┐
       │  post   │
       ├─────────┤
       │ id (PK) │
       │ thread* │
       │ author* │
       │ parent* │◄─┐
       │ content │  │
       │ *fecha  │  │
       └─────────┘  │
            │       │ (auto-referencia)
            └───────┘
```

---

**Diagrama creado el 28 de Noviembre de 2025**  
**Versión**: 1.0
